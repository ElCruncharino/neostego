pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'openstego'
include 'core', 'desktop', 'android', 'compose-desktop'
